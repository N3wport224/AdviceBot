/** Shared auth constants — safe to import from client components. */

/** Header the client sends the access password in on every API call. */
export const ACCESS_HEADER = "x-access-password";

/** localStorage key holding the password so PWA launches stay unlocked. */
export const ACCESS_STORAGE_KEY = "sabrina-access-password";
